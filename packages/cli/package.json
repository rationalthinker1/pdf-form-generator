{
  "name": "@pdf-form/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "generate-pdf": "./dist/index.js"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target node --external electron --external chromium-bidi --external lightningcss --external @babel/preset-typescript --external vite --external @vitejs/plugin-react --external rollup",
    "test": "bun test"
  },
  "dependencies": {
    "@pdf-form/core": "workspace:*",
    "pdf-lib": "^1.17.1",
    "playwright": "^1.50.0",
    "vite": "^6.0.0",
    "@vitejs/plugin-react": "^4.3.0"
  }
}
